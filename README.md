This will overwrite the following files:

Neovim Configuration
~/.config/JoeW

Tmux Config.
~/.tmux.conf

Vim Config.
~/.vim/
~/.vimrc

Starship Config.
~/.config/starship.toml

ZS
~/.zshrc
~/.zsh-aliases